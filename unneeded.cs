// Add first. 
 // Node<T> temp = (Node<T>) First;
            // Node<T> firstNode = new Node<T>(value, Head, temp);
            // temp.Previous = firstNode;
            // Head.Next = firstNode;
            // Count++;
            // return firstNode;


